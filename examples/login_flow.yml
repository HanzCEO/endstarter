name: Login Flow
type: test
jobs:
  - use: chrome
  - navigate: https://example.com/login
  - type:
      - "#username"
      - "testuser"
  - type:
      - "#password"
      - "secretpassword"
  - click: "#login-button"
  - assert-title: Dashboard
  - assert-element: "#welcome-message"
